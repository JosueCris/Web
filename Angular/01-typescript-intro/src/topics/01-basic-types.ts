const name = 'Josue';
let htPoints: number | string = 95; //  Ese signo sería el equivalente de los tipos genericos, en este caso la variable puede ser un number o un string.
const isAlive: boolean = true;

// name = '123';
htPoints = 'FULL';

console.log({
    name, htPoints, isAlive
});

export {}